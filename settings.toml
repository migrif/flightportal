# WiFi credentials
CIRCUITPY_WIFI_SSID = "your_wifi_network"
CIRCUITPY_WIFI_PASSWORD = "your_wifi_password"

# Bounding box for OpenSky API: lamin,lomin,lamax,lomax
# That is: south latitude, west longitude, north latitude, east longitude
# Tip: use https://opensky-network.org/api/states/all?lamin=&lomin=&lamax=&lomax=
# to test your box in a browser before deploying
# Example below is roughly Christchurch, NZ - adjust for your location
bounds_box = "-44.0,172.0,-43.0,173.0"

# Enable or disable the status NeoPixel LED on the MatrixPortal
# Shows yellow while connecting, green when connected, red on error
# Set to "False" if you find it distracting
status_leds = "True"
